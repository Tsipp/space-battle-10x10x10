# Test plan — UI overhaul, Task 3 (battle log)

## What changed
Плоский журнал «T 1: 🎯 Team A Артиллерия A1 → Team B Прыгун B1 @(4,6,4)»
(один цвет, без группировки) заменён на:

- Группировка по ходам с заголовками `── Ход N (A:Xdmg/Y✖ · B:… · C:…)`.
- Цветные теги команд: `A` голубой, `B` коралловый, `C` мятный (из
  `TEAM_COLORS`).
- Иконка-префикс зависит от типа события: `🎯` попадание, `💀` обычное
  убийство, `💥 ⚡таран` — таран, `🕷 Мина(A)` — мина, `🎭 раскрыл` —
  голограмма.
- Урон отображается как `−NHP` в янтарном (hit) или красном жирном
  (killed); при убийстве добавляется `✖УБИТ` красным.

## Primary flow (один adversarial прогон)

**Action**: `/usr/bin/python3 demo_ui.py log` (создано в demo_ui.py,
покрывает все 5 вариантов: hit, kill, ram, mine, hologram, двойное
событие на одном ходе).

### Assertions (всё должно быть в одном скриншоте)

1. **Заголовок ход 1**: содержит текст `── Ход 1` в голубом, сводку
   `(A:1dmg · B:1dmg)` в серой мелкой кнопке. Если вместо «Ход 1» —
   «T 1» или заголовок отсутствует, группировка сломана → FAIL.
2. **Цвет команд**: буква `A` после иконки попадания окрашена в голубой
   (#4a9dff), `B` в коралловый (#ff5c7a), `C` в мятный (#6bff9d). Если
   все буквы одного цвета или белые — тегирование сломано → FAIL.
3. **Таран (ход 2)**: строка содержит и иконку `💥`, и текст `⚡таран` в
   фиолетовом, с отметкой `✖УБИТ` красным. Без `⚡таран` в фиолетовом
   — логика `ev.get('ram')/type=='ram_kill'` сломана → FAIL.
4. **Мина (ход 3)**: строка содержит `🕷 Мина(A)` (владелец команды A
   голубым), стрелку к `B Прыгун B1`, `−2HP` в красном, `✖УБИТ`. Без
   «Мина(A)» конструкция `ev.get('type') == 'mine_detonated'` не
   учтена → FAIL.
5. **Голограмма (ход 4)**: содержит `🎭`, текст `раскрыл` в фиолетовом
   и `голограмму` с владельцем `A` в голубом. Без этого ветка
   `hologram_destroyed` не работает → FAIL.
6. **Сводка хода 3**: должна показать суммы из двух событий, т.е.
   `A:2dmg/1✖ · B:1dmg/1✖` (мина A +2/kill, артиллерия B +1/kill).
   Если сумма 0 или нет обеих команд — аггрегатор `dmg/kills` по
   командам сломан → FAIL.

## Screenshot plan
Один скриншот окна 760×520 с полностью видимыми ходами 1–4. Все 6
assertions должны быть верифицируемы на нём.
